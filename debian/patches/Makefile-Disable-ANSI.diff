Disable ANSI behavior when using gcc.
--- a/Makefile
+++ b/Makefile
@@ -9,8 +9,8 @@
 # and glk.h, glkstart.h, and Make.glkterm in the include directory.
 
 # Pick a C compiler.
-#CC = cc
-CC = gcc -ansi
+CC = cc
+#CC = gcc -ansi
 
 # You may need to set directories to pick up the ncurses library.
 #INCLUDEDIRS = -I/usr/5include
